# Build outputs
/build/
*.exe
*.aar
*.xcframework
*.dll
*.so
*.dylib

# Go
*.test
*.test.exe
*.out
# Nota: go.sum SÍ se commitea — CI (GitHub Actions) hace checkout limpio y
# sin go.sum falla con "missing go.sum entry" (Go usa -mod=readonly por
defecto desde 1.16).

# IDE
.idea/
.vscode/
*.swp
*.swo

# Binary dependencies (downloaded at runtime)
/bin/
/ffmpeg*
/yt-dlp*

# Temp files
*.tmp
*~
