# Flutter
build/
.dart_tool/
.packages
.pub/
.pub-cache/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.metadata

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/gradle/wrapper/gradle-wrapper.jar
android/app/libs/bitly.aar
android/app/libs/bitly-sources.jar
android/app/libs/backend.aar
android/app/libs/backend-sources.jar
android/app/libs/spotiflac.aar
android/app/libs/spotiflac-sources.jar
android/app/src/main/java/io/flutter/plugins/GeneratedPluginRegistrant.java

# Go
bitly-backend.exe
**/bitly-backend.exe
bitly-backend-linux
bitly-backend-macos
bitly-backend-macos-arm64
bitly.db
bitly.db-shm
bitly.db-wal
bitly_server.exe
linux/runner/bitly-backend

# Python
__pycache__/
*.pyc
.ruff_cache/

# OS
.DS_Store
Thumbs.db
nul

# Env
.env

# Node
node_modules/

# temp
temp_*/

# logs
*.log
*.hprof
build_error.txt
error_log.txt

# Build artifacts
*.zip
# Solo el dir raíz (copias de extensiones empaquetadas). Anclado con /
# para NO ignorar go_backend/internal/extensions/ (código Go fuente).
/extensions/
flutter_*.png
assets/extensions_bundle.zip

# Scripts locales de build (no versionar): se listan por tipo para no
# dejar TODO scripts/ invisible, que hacía que un .sh nuevo no se viera.
scripts/__pycache__/
scripts/*.py
scripts/*.bat
scripts/*.ps1
# Claves del sistema de códigos: nunca al repo (ver scripts/generate_keys.py)
/scripts/keys/

# old doc files
*.md
!README.md
!knowledge.md

# Root build artifacts (regenerable via go_backend/build_all.sh)
/*.aar
/*.jar
/bitly-backend-windows.exe
*.exe~
# Solo el dir raíz. Anclado con / para NO ignorar go_backend/internal/bin/
# (código Go fuente del gestor de binarios).
/bin/
signed_sessions/
null

# Secrets (archivo real con token — NUNCA subir; el template es
# secretos.example.dart y el CI lo genera)
lib/config/secrets.dart

# Stores de sesión de las extensiones: el backend los escribe en runtime
# (extID + "_store.json", ver go_backend/internal/extensions/storage.go) y
# contienen tokens reales. Los scripts de release ya los borran del paquete;
# acá evitamos que un store de desarrollo vuelva a entrar al repo.
*_store.json
lib/config/secretos.dart

# Keystore de release + credenciales de firma — NUNCA versionar.
# Si se filtran, cualquiera puede firmar apps en tu nombre; si se pierden,
# no podrás volver a actualizar la app. Backup fuera del repo.
/android/key.properties
/android/keystore/
*.jks
*.keystore

# Backups locales (no versionar)
*.rar

# Build outputs for sharing (Drive) — keep out of git
/APKs Bitly/

# Binarios del release (APKs/instaladores) — se suben a la GitHub Release,
# nunca al repo
/dist/

# Capturas de prueba en la raíz (emuladores/PC) — no versionar
/emul*.png
/shot*.png
/setup*.png
/pc_*.png
/s*.png
/error_texto*.png

# ── Debug / temp files ──────────────────────────────────────────────
tmp_*
probe_*
dying_url.txt
fresh_*
trace_*
urlapp.txt
ytp.json
test_url.txt
itag*_url.txt
APK_INSTALLATION_GUIDE.txt
installed_base.apk
prefs_check.xml
prefs_dump.xml
go_backend/all_ext_test.go
