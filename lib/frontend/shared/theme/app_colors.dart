import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand core (both modes) ──
  static const primary = Color(0xFF1ED760);
  static const darkGreen = Color(0xFF071A0D);

  // ── Dark mode: neon / vibrant ──
  static const greenBright = Color(0xFF5AF13D);
  static const greenNeon = Color(0xFF3FEF38);
  static const greenPale = Color(0xFFBFEFC3);

  // ── Light mode: premium / deep ──
  static const greenDeep = Color(0xFF0B5A1E);
  static const greenMedium = Color(0xFF118C33);
  static const greenLight = Color(0xFF22D64A);

  // ── Backgrounds ──
  static const bgDark = Color(0xFF000000);
  static const bgLight = Color(0xFFFFFFFF);

  // ── Surface / Sheet backgrounds (dark / light pair) ──
  static const surfaceDark = Color(0xFF1A1A1A);
  static const surfaceLight = Color(0xFFF5F5F5);
}

