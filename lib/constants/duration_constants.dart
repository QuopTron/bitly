class DurationConstants {
  static const Duration debounce = Duration(milliseconds: 300);
  static const Duration searchDebounce = Duration(milliseconds: 500);
  static const Duration animationFast = Duration(milliseconds: 200);
  static const Duration animationNormal = Duration(milliseconds: 350);
  static const Duration animationSlow = Duration(milliseconds: 600);
  static const Duration autoScanDelay = Duration(milliseconds: 500);
  static const Duration providerWarmup = Duration(milliseconds: 400);
  static const Duration preloadThreshold = Duration(seconds: 30);
  static const Duration progressPollInterval = Duration(seconds: 1);
  static const Duration playlistAutoSave = Duration(seconds: 5);
}